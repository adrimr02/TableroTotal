<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TableroTotal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.TableroTotal" parent="Base.Theme.TableroTotal" />
    <style name="Theme.TableroTotal.Fullscreen" parent="Theme.TableroTotal">
        <item name="android:actionBarStyle">@style/Widget.Theme.TableroTotal.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.TableroTotal.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>